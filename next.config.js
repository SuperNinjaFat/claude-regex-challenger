/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Empty turbopack config to silence the warning (we'll use default Turbopack)
  turbopack: {},
}

module.exports = nextConfig
